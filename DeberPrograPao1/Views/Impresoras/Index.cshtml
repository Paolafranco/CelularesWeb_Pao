@model IEnumerable<DeberPrograPao1.Models.Impresora>

@{
    ViewData["Title"] = "Impresoras";
}

    <h1>Impresoras</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Marca)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Modelo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var impresora in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => impresora.Marca)
            </td>
            <td>
                @Html.DisplayFor(modelItem => impresora.Modelo)
            </td>
            <td>
                <form action="impresoras/comprar" method="post">
                    <div class="row">
                        <input name="id" type="hidden" value="@impresora.Id" />
                        <input type="submit" value="Comprar" class="btn btn-success" />
                    </div>
                </form>
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@impresora.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@impresora.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@impresora.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
